diff --git a/src/core/lv_obj_pos.c b/src/core/lv_obj_pos.c
index 177b0fb5b..91dc1a8a8 100644
--- a/src/core/lv_obj_pos.c
+++ b/src/core/lv_obj_pos.c
@@ -17,6 +17,10 @@
 #include "lv_refr_private.h"
 #include "../core/lv_global.h"
 
+/* Telemetry hook provided by helix_lvgl_anomaly.cpp — reports LVGL anomalies
+ * with backtrace + device context for remote crash diagnostics. */
+extern void helix_lvgl_anomaly(const char * code, const char * context);
+
 /*********************
  *      DEFINES
  *********************/
@@ -1040,6 +1044,10 @@ static lv_obj_tree_walk_res_t blur_walk_cb(lv_obj_t * obj, void * user_data)
         for(i = 0; i < obj->style_cnt; i++) {
             lv_obj_style_t * obj_style = &obj->styles[i];
             if(obj_style->is_disabled) continue;
+            if(obj_style->style == NULL) {
+                helix_lvgl_anomaly("obj_pos_null_style", "blur_walk_cb: obj_style->style is NULL");
+                continue;
+            }
 
             lv_state_t state_style = lv_obj_style_get_selector_state(obj->styles[i].selector);
             if((state_style & state_inv)) continue;
@@ -1328,7 +1336,13 @@ static lv_result_t obj_invalidate_area_internal(const lv_display_t * disp, const
     lv_result_t res = invalidate_area_core(obj, &area_tmp);
     if(res == LV_RESULT_INVALID) return res;
 
-    /*If this area is on a blurred widget, invalidate that widget too*/
+    /*If this area is on a blurred widget, invalidate that widget too.
+     *
+     *Disabled unconditionally — HelixScreen never uses LV_STYLE_BLUR_RADIUS
+     *or LV_STYLE_DROP_SHADOW_OPA.  The recursive O(n) tree walk on every
+     *invalidation is pure overhead and crashes when any lv_obj_t in the
+     *tree has corrupted style pointers (#820, #528, #417, #419, #420).*/
+#if 0
     blur_walk_data_t blur_walk_data;
     blur_walk_data.requester_obj = obj;
     blur_walk_data.inv_area = &area_tmp;
@@ -1337,6 +1351,7 @@ static lv_result_t obj_invalidate_area_internal(const lv_display_t * disp, const
     lv_obj_tree_walk(disp->sys_layer, blur_walk_cb, &blur_walk_data);
     lv_obj_tree_walk(disp->top_layer, blur_walk_cb, &blur_walk_data);
     lv_obj_tree_walk(disp->bottom_layer, blur_walk_cb, &blur_walk_data);
+#endif
 
     return res;
 }
@@ -1530,10 +1545,18 @@ static int32_t calc_content_height(lv_obj_t * obj)
 
 static void layout_update_core(lv_obj_t * obj)
 {
+    if(obj == NULL) {
+        helix_lvgl_anomaly("obj_pos_null_style", "layout_update_core: obj is NULL");
+        return;
+    }
     uint32_t i;
     uint32_t child_cnt = lv_obj_get_child_count(obj);
     for(i = 0; i < child_cnt; i++) {
         lv_obj_t * child = obj->spec_attr->children[i];
+        if(child == NULL) {
+            helix_lvgl_anomaly("obj_pos_null_style", "layout_update_core: child is NULL");
+            continue;
+        }
         layout_update_core(child);
     }
 
