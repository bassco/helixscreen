diff --git a/src/core/lv_global.h b/src/core/lv_global.h
index 769528a2b..979e35648 100644
--- a/src/core/lv_global.h
+++ b/src/core/lv_global.h
@@ -114,6 +114,7 @@ typedef struct _lv_global_t {
     uint32_t math_rand_seed;

     lv_event_t * event_header;
+    uint32_t event_depth;               /**< Push/pop nesting depth for corruption detection */
     uint32_t event_last_register_id;

     lv_timer_state_t timer_state;
diff --git a/src/core/lv_obj.c b/src/core/lv_obj.c
index 3ba67b92f..a4b2addda 100644
--- a/src/core/lv_obj.c
+++ b/src/core/lv_obj.c
@@ -602,6 +602,7 @@ static void lv_obj_constructor(const lv_obj_class_t * class_p, lv_obj_t * obj)
 static void lv_obj_destructor(const lv_obj_class_t * class_p, lv_obj_t * obj)
 {
     LV_UNUSED(class_p);
+    if(obj == NULL) return;

     lv_event_mark_deleted(obj);

diff --git a/src/core/lv_obj_tree.c b/src/core/lv_obj_tree.c
index 2dc7a024f..22e8f5a1f 100644
--- a/src/core/lv_obj_tree.c
+++ b/src/core/lv_obj_tree.c
@@ -646,7 +646,10 @@ void lv_obj_dump_tree(lv_obj_t * start_obj)

 static void lv_obj_delete_async_cb(void * obj)
 {
-    LV_ASSERT_OBJ(obj, MY_CLASS);
+    /*Guard against use-after-free: if the object was already freed
+     *(e.g. parent deletion recursed into children before the child's
+     *pending async delete could be cancelled), skip the deletion.*/
+    if(!lv_obj_is_valid(obj)) return;

     lv_obj_delete(obj);
 }
@@ -667,7 +670,7 @@ static void obj_indev_reset(lv_indev_t * indev, lv_obj_t * obj)

 static void obj_delete_core(lv_obj_t * obj)
 {
-    if(obj->is_deleting)
+    if(obj == NULL || obj->is_deleting)
         return;

     obj->is_deleting = true;
@@ -682,9 +685,22 @@ static void obj_delete_core(lv_obj_t * obj)
     /*Clean registered event_cb*/
     if(obj->spec_attr) lv_event_remove_all(&(obj->spec_attr->event_list));

-    /*Recursively delete the children*/
+    /*Cancel any pending async deletions for this object first, before
+     *recursing into children. This prevents double-free when both parent
+     *and child have pending lv_obj_delete_async calls.*/
+    lv_result_t async_cancel_res = LV_RESULT_OK;
+    while(async_cancel_res == LV_RESULT_OK) {
+        async_cancel_res = lv_async_call_cancel(lv_obj_delete_async_cb, obj);
+    }
+
+    /*Recursively delete the children, cancelling any pending async
+     *deletions for each child before recursing to prevent use-after-free.*/
     lv_obj_t * child = lv_obj_get_child(obj, 0);
     while(child) {
+        lv_result_t child_cancel_res = LV_RESULT_OK;
+        while(child_cancel_res == LV_RESULT_OK) {
+            child_cancel_res = lv_async_call_cancel(lv_obj_delete_async_cb, child);
+        }
         obj_delete_core(child);
         child = lv_obj_get_child(obj, 0);
     }
@@ -713,12 +729,6 @@ static void obj_delete_core(lv_obj_t * obj)
         indev = lv_indev_get_next(indev);
     }

-    /*Delete all pending async del-s*/
-    lv_result_t async_cancel_res = LV_RESULT_OK;
-    while(async_cancel_res == LV_RESULT_OK) {
-        async_cancel_res = lv_async_call_cancel(lv_obj_delete_async_cb, obj);
-    }
-
     /*All children deleted. Now clean up the object specific data*/
     lv_obj_destruct(obj);

diff --git a/src/misc/lv_event.c b/src/misc/lv_event.c
index 39c17ffd1..a5e7c3f02 100644
--- a/src/misc/lv_event.c
+++ b/src/misc/lv_event.c
@@ -17,6 +17,7 @@
  *********************/

 #define event_head LV_GLOBAL_DEFAULT()->event_header
+#define event_depth LV_GLOBAL_DEFAULT()->event_depth
 #define event_last_id LV_GLOBAL_DEFAULT()->event_last_register_id

 /**********************
@@ -74,12 +75,13 @@ void lv_event_push(lv_event_t * e)
      *called from this `event_cb`.*/
     e->prev = event_head;
     event_head = e;
-
+    event_depth++;
 }

 void lv_event_pop(lv_event_t * e)
 {
     event_head = e->prev;
+    if(event_depth > 0) event_depth--;
 }

 lv_result_t lv_event_push_and_send(lv_event_list_t * event_list, lv_event_code_t code, void * original_target,
@@ -317,9 +319,32 @@ uint32_t lv_event_register_id(void)

 void lv_event_mark_deleted(void * target)
 {
+    if(target == NULL) return;
+
     lv_event_t * e = event_head;

-    while(e) {
+    /*If no events are being dispatched (depth == 0), event_head MUST be
+     *NULL. A non-NULL value means something corrupted it — heap overflow,
+     *wild write, or a leaked push without pop. Reset and bail to prevent
+     *SIGSEGV from dereferencing garbage. Log the corruption for diagnosis.*/
+    if(e && event_depth == 0) {
+        LV_LOG_ERROR("event_head corruption: %p with depth=0 (target=%p), resetting", (void *)e, target);
+        event_head = NULL;
+        return;
+    }
+
+    /*Depth limit prevents infinite loops from corrupt circular chains.
+     *Event dispatch depth > 64 is abnormal (typical max is ~3-5 nested
+     *events). A corrupt prev pointer that happens to be aligned and
+     *mapped would be followed indefinitely without this.*/
+    int depth = 64;
+    while(e && depth-- > 0) {
+        /*Events are stack-allocated structs — always pointer-aligned.
+         *A misaligned pointer means the chain is corrupt (heap/stack
+         *corruption stomped event_head or a prev pointer). Bail out
+         *to avoid SIGSEGV from dereferencing garbage.*/
+        if((lv_uintptr_t)e & (sizeof(void *) - 1)) break;
+
         if(e->original_target == target || e->current_target == target) e->deleted = 1;
         e = e->prev;
     }
